import React from "react";
import { Layout } from "@ui-kitten/components";
import QuicksandText from "../Common/QuicksandText";

interface BottomBarHeaderProps {}

const BottomBarHeader = (props: BottomBarHeaderProps) => {
  return (
    <Layout>
      <QuicksandText></QuicksandText>
    </Layout>
  );
};

export default BottomBarHeader;
