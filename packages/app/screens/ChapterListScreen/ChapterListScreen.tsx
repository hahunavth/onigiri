import { View, Text } from "react-native";
import React from "react";
import { ChapterListBottomSheet } from "../../components/ChapterListBottomSheet";

type Props = {};

export const ChapterListScreen = (props: Props) => {
  return <ChapterListBottomSheet />;
};
