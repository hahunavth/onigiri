import { View, Text } from "native-base";
import React from "react";

type Props = {};

export const ComicListScreen = (props: Props) => {
  return (
    <View>
      <Text>ComicListScreen</Text>
    </View>
  );
};
