import { View, Text } from "react-native";
import React from "react";

type Props = {};

export const Loading = (props: Props) => {
  return (
    <View>
      <Text>Loading</Text>
    </View>
  );
};
